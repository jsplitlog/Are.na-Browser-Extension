node_modules/
*.local
.DS_Store

# Build output (see docs/cross-browser-plan.md, WS4): per-target dist/<target>/
# folders and packaged zips. Not committed — see the WS4 report for the
# tradeoff this has with README's current "Download ZIP" install flow.
dist/
